{
  "name": "restroworks-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:cms": "npm run dev:local --workspace @restroworks/cms",
    "dev:web": "npm run dev --workspace @restroworks/web",
    "dev": "npm run dev:cms & npm run dev:web"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "dependencies": {}
}
